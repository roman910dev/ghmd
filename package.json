{
  "name": "ghmd-dev",
  "version": "1.0.1",
  "description": "This is not a package itself. It just defines an environment to develop and test the `ghmd` package. It is available for python and node, both can be found under the `src` directory.",
  "scripts": {
    "dev:browser": "pnpm --dir ./src/browser dev",
    "build:browser": "pnpm --dir ./src/browser build",
    "build:node": "pnpm --dir ./src/node build",
    "format:node": "biome format --write",
    "format:python": "cd ./src/python && uv run ruff format",
    "format": "pnpm format:node & pnpm format:python",
    "postinstall": "if [ \"$CI\" != \"true\" ]; then cd ./src/python && uv sync --all-groups; fi",
    "prepare": "pnpm sync-shared && pnpm build:node && pnpm build:browser && ncp README.md src/node/README.md && ncp README.md src/python/README.md",
    "sync-shared": "node scripts/sync-shared.js && pnpm format",
    "test": "vitest run"
  },
  "packageManager": "pnpm@10.18.3",
  "devDependencies": {
    "@biomejs/biome": "^2.4.5",
    "ncp": "^2.0.0",
    "rimraf": "^6.1.3",
    "vitest": "^4.0.18"
  },
  "type": "module"
}
